#include <iostream>

using namespace std;

#define ABS(number) ((number)<0 ? -(number): (number))
#define greater(num1, num2) ((num1>num2) ? (num1) : (num2))
int main ()
{
    cout<<ABS(-27);
    cout<<endl;
    cout<<greater(5,9);
    return 0;
}
